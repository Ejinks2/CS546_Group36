<div class="header">
    <h1>{{title}}</h1>
    <h2>{{description}}</h2>
    <nav>
        <a href="/">Home</a>
        <a href="/admin">Admin</a>
        <a href="/search">Search</a>
        <a href="/report">Report</a>
        {{#if user}}
            <a href="/logout">Logout</a>
        {{/if}}
    </nav>
</div>