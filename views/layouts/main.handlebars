<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{{ title }}</title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class="header">
            <h1>{{title}}</h1>
            <h2>{{description}}</h2>
            <nav>
                {{#each links}}
                <a href={{this}}>{{@key}}</a>
                {{/each}}
                {{#if user}}
                    <a href="/logout">Logout</a>
                {{/if}}
            </nav>
        </div>
        {{{ body }}}
    </body>
</html>